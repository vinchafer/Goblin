# Sprint 10.6 — WIP Log

Branch: master · Start HEAD: 9998d1c · Quality bar 9.5/10

## Phase 0 — Pre-flight ✅
- localhost:3000 → 200. git HEAD = 9998d1c. ✅
- CDP 9222 → connection refused (will use browser-harness daemon for walks). ⚠️
- Read inputs + traced OAuth, Send-to-Code, settings code paths.

## Item 10.6-1 — GitHub OAuth — IN PROGRESS
ROOT CAUSE: `/api/connectors/status` does not exist (mounted nowhere). The active
ConnectorsPage modal fetches it → 404 → GitHub always renders "Verbinden" even when the
token is persisted. Secondary: double-step bounce + missing `returnTo` → lands /dashboard
(or /login on domain mismatch). "Lands on login" prime suspect = Railway `NEXT_PUBLIC_APP_URL`
domain ≠ login-cookie domain (founder env). Full trace: sprint-10-6/oauth-investigation.md.
Fixes: ConnectorsPage → real /api/github/status; direct OAuth init w/ returnTo;
legacy button returnTo; callback logging.
