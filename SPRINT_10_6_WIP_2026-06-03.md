# Sprint 10.6 — WIP Log

Branch: master · Start HEAD: 9998d1c · Quality bar 9.5/10

## Phase 0 — Pre-flight ✅
- localhost:3000 → 200. git HEAD = 9998d1c. ✅
- CDP 9222 → connection refused (will use browser-harness daemon for walks). ⚠️
- Read inputs + traced OAuth, Send-to-Code, settings code paths.

## Item 10.6-1 — GitHub OAuth — IN PROGRESS
ROOT CAUSE: `/api/connectors/status` does not exist (mounted nowhere). The active
ConnectorsPage modal fetches it → 404 → GitHub always renders "Verbinden" even when the
token is persisted. Secondary: double-step bounce + missing `returnTo` → lands /dashboard
(or /login on domain mismatch). "Lands on login" prime suspect = Railway `NEXT_PUBLIC_APP_URL`
domain ≠ login-cookie domain (founder env). Full trace: sprint-10-6/oauth-investigation.md.
Fixes: ConnectorsPage → real /api/github/status; direct OAuth init w/ returnTo;
legacy button returnTo; callback logging.

## Item 10.6-1 — DONE (230ef85)
ConnectorsPage → /api/github/status; one-click OAuth w/ returnTo; legacy
button returnTo; callback step-logging. Login-bounce prime suspect = Railway
NEXT_PUBLIC_APP_URL domain mismatch (founder env). Live verify by founder.

## Item 10.6-2 — DONE (24f3059)
blocksToFiles() splits multi-block sends into real files (index.html/style.css/
script.js), names CSS/JS to match HTML refs, strips // File: comments. Wired
through multi-session (session drafts) + classic paths. Single-block unchanged.
Fixture test 6/6 pass. typecheck green.

## Item 10.6-3 — Vercel canonical URL — IN PROGRESS

## Item 10.6-3 — DONE (e9e6f68)
Poll getDeployStatus until READY in deploy SSE → surfaces canonical alias +
200-answering URL. Trace: sprint-10-6/vercel-url-trace.md.

## Item 10.6-4 — DONE
Found+fixed: new-project create navigated to hub (chat), so stashed stc-pending
never reached CodeWorkspace. pendingStcTab() helper → /work?tab=code when stc
pending. All create paths patched. typecheck green. Live CDP walk deferred
(no Chrome debug port locally). Evidence: sprint-10-6/stc-no-project/VERIFY.md.

## Item 10.6-5 — Vercel ownership UX — IN PROGRESS

## Item 10.6-5 — DONE
Three ownership touches: onboarding card+callout (signup link), pre-deploy
explainer modal (classic) + friendly no-token error (both paths) + confirm-modal
ownership line, Settings Vercel italic note. typecheck web+api green. Visual
verify deferred (no CDP). Evidence: sprint-10-6/vercel-ownership/VERIFY.md.
