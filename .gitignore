node_modules
*.txt
supabase/.temp
package-lock.json
.next
dist
# Env files — never commit secrets
.env
.env.local
.env.*.local
.env.development
.env.production
.env.test
.env.staging

# Allow examples only
!.env.example
!.env.*.example
*.log
.DS_Store
.cache
turbo
.vercel
*.tsbuildinfo
playwright-report/
test-results/

# Personal testing-adjustment docs (Word/PDF/screenshots — not versioned)
Anpassungen nach Testing/
# Downloaded production HTML snapshot (reference only, build artifact)
vercel_live.html
