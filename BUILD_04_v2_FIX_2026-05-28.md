# Build 04 v2 — Composition Fix Report

**Date:** 2026-05-28
**Target:** `built_04_v2.html` — empty block top-anchored (upper third), not vertically centered.
**Addresses:** BUILD_04_v2_IMPL_2026-05-28.md §10 flags #1 (top-anchor) + #2 (align-items on message list).

## 1. Files changed

| File | Why |
|------|-----|
| `apps/web/components/chat/EmptyChat.tsx` | Remove layout-controlling styles (`flex:1`, `justifyContent:center`, `padding:40px 24px`) from outer wrapper so the parent's `padding-top` governs position. |
| `apps/web/app/globals.css` | Relocate `align-items:center` from `.chat-scroll` base to `.chat-scroll--empty` so the message list (screen 05) keeps default `stretch`. |

No other files touched. standalone-chat.tsx, Header.tsx, composer/ChatInput/Message untouched.

## 2. EDIT 1 — EmptyChat.tsx outer wrapper

Wrapper element: outer `<div>` at **L26**, opening `<div style={{ ... }}>` inside `EmptyChat({ onSuggestionClick })`.

**Style prop BEFORE:**
```tsx
style={{
  flex: 1,
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  justifyContent: 'center',
  padding: '40px 24px',
  width: '100%',
}}
```

**Style prop AFTER:**
```tsx
style={{
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  width: '100%',
}}
```

Three properties removed (`flex: 1`, `justifyContent: 'center'`, `padding: '40px 24px'`). `display`, `flexDirection`, `alignItems`, `width` kept. **No property added.** Children (mark → greeting → chips) unchanged in DOM order; `alignItems: 'center'` retained → wrapper still horizontally centers its children. Inner `.empty-block` div (maxWidth 640) untouched.

## 3. EDIT 2 — globals.css

**`.chat-scroll` BEFORE:**
```css
.chat-scroll {
  flex: 1;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 24px;
}
```
**`.chat-scroll` AFTER:**
```css
.chat-scroll {
  flex: 1;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  padding: 24px;
}
```

**`.chat-scroll--empty` BEFORE:**
```css
.chat-scroll--empty {
  justify-content: flex-start;
  padding-top: 120px;
}
```
**`.chat-scroll--empty` AFTER:**
```css
.chat-scroll--empty {
  justify-content: flex-start;
  padding-top: 120px;
  align-items: center;
}
```

`align-items: center` relocated base → modifier. **Mobile @media block unchanged:**
```css
@media (max-width: 640px) {
  .chat-scroll--empty {
    padding-top: 64px;
  }
}
```

## 4. Verification

- `.chat-scroll` base now has 5 properties, **no `align-items`**. ✓
- `.chat-scroll--empty` has 3 properties incl. `align-items: center`. ✓
- EmptyChat wrapper: `flex`, `justifyContent`, `padding` absent from style prop (verbatim in §2). ✓
- Emoji grep `apps/web/components/chat/` + `apps/web/app/dashboard/chat/`: **No matches found.** ✓

**Visual trace:**
- **Empty state:** EmptyChat = content-fit box (no `flex:1`). Parent `.chat-scroll.chat-scroll--empty` → `justify-content:flex-start` + `padding-top:120px` (64px mobile) + `align-items:center`. Box sits 120/64px below top, horizontally centered. Matches v2.
- **Non-empty (screen 05):** Parent `.chat-scroll` only → no `align-items` (defaults `stretch`), no `justify-content` (column defaults to top). Message rows stretch full width, self-align internally. No regression.

## 5. Build status

- `pnpm --filter @goblin/web typecheck` → **PASS** (tsc --noEmit, no errors).
- `pnpm --filter @goblin/web build` → **PASS** (full route table, no error).

## 6. Surprising

Nothing. Both flags from the prior pass resolved with pure removals + one relocation; no new property added to either file. Empty block now top-anchored per v2; message-list base reverts to `stretch`.
