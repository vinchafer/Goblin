# TIMINGS — Feeling Walk 2

No timings captured. No authenticated browser session was established
(see ENV_BLOCKER.md), so no TTFT, step-stream, deploy, or interaction
timings exist. No values are invented.

Only non-session timing observed (control probe, curl through sanctioned proxy):
- GET https://www.justgoblin.com/ (follow 307→www): HTTP 200 in ~0.86 s
  (source: raw/proxy-control-proofs.txt)
