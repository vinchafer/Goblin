# Sprint 9 — WIP Log (2026-06-02)

Re-audit + Convergence Architecture. Budget 10-14h. Quality bar 9.5.

## Running log

- [start] Phase 0 pre-flight: CDP :9222 OK (Chrome 148), localhost:3000 → 200, HEAD=1c0781d. sprint-9/ dirs created.
- [start] Read SESSION_HANDOFF_2026-06-02 + THREE_PERSONA_AUDIT_SYNTHESIS. P0 = model fix (now claimed fixed by Vincent: Groq + Gemini BYOK).
- [start] Browser harness verified — app title "Goblin — The cloud workshop for builders".

## Phase 0 — pre-flight + model verification (DONE, with critical finding)

- localhost:3000 page CANNOT reach prod Railway API (CORS): `fetch(railway/health)` → "Failed to fetch". Real users on goblin-web.vercel.app are fine (origin allowlisted). → Audits run on **prod** (goblin-web.vercel.app), not localhost. Localhost reserved for Phase-4 code verification (needs local API :3001).
- Railway API healthy: `/health` → 200, version 0.2.0.
- Logged into prod as test user (vinc.hafner3@gmail.com).
- **MODEL GATE (5.2):**
  - ✅ **Groq Llama 3.3 70B (BYOK)** — streamed full Hello-World HTML response. WORKS. → Sprint 9 proceeds.
  - 🔴 **Gemini 2.5 Pro (BYOK, marked ACTIVE/default)** → `400 litellm.BadRequestError: LLM Provider NOT provided ... model=google/gemini-2.5-pro`. Wrong LiteLLM provider prefix (`google/` should be `gemini/`).
  - 🔴 **Gemini 2.0 Flash (free, the default new users hit)** → "Model not found in LiteLLM" (not registered in proxy).
- **CRITICAL FINDING:** Model fix is PARTIAL. Vincent's claim "Groq + Gemini both working" is FALSE — only Groq works. The DEFAULT model a fresh user lands on is still broken → the exact Sprint-8 DOA failure repeats for trial users who don't manually pick Groq.
- Root cause: prod model catalog (DB, not repo seed) has Gemini slug `google/gemini-2.5-pro`; repo `providers.ts` correctly uses `gemini/` prefix. Prod diverged (Vincent added rows manually). Free Gemini not registered in LiteLLM proxy at all.
- Founder action: fix prod model rows `google/gemini-2.5-pro` → `gemini/gemini-2.5-pro`; register/repair free Gemini in LiteLLM. Cannot fix from repo (prod DB state, B3 shield).
- E2E-TEST pollution confirmed: 20+ `[E2E-TEST]` projects in sidebar (P1 scrub).
- KEYBOARD SHORTCUTS overlay confirmed present + undismissed on dashboard + chat (P1).

## Phase 1 — Max fresh-signup account (DONE)
- Created prod user vinc.hafner4@gmail.com (id 6642e2d4-5564-48ea-ab22-85cf2a212257), password MaxBerlin#2026, email_confirm=true, NO BYOK keys. Script: apps/api/src/scripts/create-max-test-user.ts.
- Onboarding walked (prod): 5-step flow. Step 01 = "How should Goblin talk to AI?" — PATH B (RECOMMENDED, guided free key, ~2min) vs PATH A (paste own key, ~60s) vs "Explore first". Plain-language, no card, encrypted-keys reassurance. Genuinely strong — matches Vincent's "top setup assistant" claim.
- Step 02 = "Pick your AI provider" — #1 RECOMMENDED·FREE TIER = **Google Gemini · GEMINI 2.5 PRO · free tier** ("gentlest start... 1,500 req/day, no card... auto-routes through Goblin's encrypted proxy"). Plain key-acquisition steps (aistudio.google.com → Get API key → Create → paste).
- 🔴🔴 **COMPOUND FINDING:** the onboarding's #1 recommended destination IS the broken model. A fresh Max who follows the gentle guided path gets a *valid* Google key, then his first prompt still dies with `400 LLM Provider NOT provided (google/gemini-2.5-pro)`. Beautiful funnel → dead end. Onboarding UX 9/10; destination 0/10.
- Did NOT fetch a real Google key (§6.2 no-keys; model broken anyway). Evidence sufficient.

## Phase 2 — v2 audits (in progress)
- Switching to test user (vinc.hafner3, has working Groq key) for Code Tab core-loop + Sofia evidence.
- Evidence captured: landing (EN, strong), login, dashboard (default model=broken Gemini Flash, 58 E2E projects), onboarding (5-step, excellent, recommends broken Gemini), chat loop (Groq ✅ / Gemini ✗), Code Tab (multi-session live, Groq code-gen ✅ clean HTML, Save/Publish, draft state), ⌘K=nav quick-switcher not dev palette, no git surface.
- 4 docs written: DARIO/MAX/SOFIA _v2 + SYNTHESIS_v2. Verdict: ~70% default-path / ~90% working-path beta-ready; one P0 (repoint model) closes the gap.

## Phase 3 — Convergence Architecture (DONE)
- CONVERGENCE_ARCHITECTURE_2026-06-02.md (A–G) + 5 mockups in sprint-9/convergence/. Principle: One Canvas, Progressive Reach (intent foreground + gesture depth, no mode). Rendered-checked.

## Phase 4 — P1 polish (DONE)
- 9.1 shortcuts overlay (?-toggle/touch-hidden/no auto-pop), 9.5 ?session= deep-link, 9.3 hide [E2E-TEST] (filter on read), 9.4 friendlyError plain-German + jargon scrub, 9.2 (partial) DE app copy + dashboard E2E filter. 9.2 full i18n sweep deferred to Sprint 10.
- typecheck clean; next build exit 0. 4 code commits + docs commit on master (not pushed).

## Phase 5 — DONE
- SPRINT_9_COMPLETE + SESSION_HANDOFF refreshed. Beta-readiness ~70% default / ~90% working path, gated on the model P0.
