# Sprint 10.7 — WIP Log

Start HEAD: 9a6237b (10.6 docs final). Branch master. Dev server up (3000).
Chrome debug port 9222 DOWN → visual CDP verification deferred; code-level
verification + typecheck/build used meanwhile. Vincent asked to launch Chrome
with --remote-debugging-port=9222 for Phase E.

## Phase A — P0 BLOCKERS

### 10.7-1 + 10.7-2  — DONE (commit 1247046)
Root cause: new Settings "Meine Keys" → Hinzufügen/Verwalten pushed the
**read-only** ApiKeysPage (no input/form/submit). Only working add form lived
in legacy AddKeyModal via old /dashboard/settings/keys route = the "alte
Einstellungen". Not a 10.6 regression per se — 10.5's migration left the new
page a dead end. API POST /api/byok-keys is fine.
Fix: new ProviderKeyForm.tsx — in-sheet Goblin-styled add/manage form, provider
preselected, POSTs to API, refetches list on return, precise errors
(invalid/timeout/network). Covers desktop+mobile (shared ModelsPage).
typecheck(web) PASS.

### 10.7-3 — DONE (commit c619c4e)
Root cause: both chat routes only surfaced upstream errors after the 120s
provider timeout = "spins forever". chat.ts (project route) also dropped
error events entirely; chat-sessions didn't propagate abort.
Fix: streamCompletionGuarded — 45s first-token watchdog, aborts dangling
upstream fetch, streams clear German error. chat.ts forwards error/
fallback_notice. Gemini's own failure is upstream LiteLLM config →
sprint-10-7/GEMINI_FOUNDER_ACTION.md (founder action per §9(d)).
tsc(api) PASS.

### 10.7-4 — regression audit (see sprint-10-7/regression-audit.md)

## Phase B — onboarding polish: DONE
10.7-5 (6ec18b1), 10.7-6/7/8 (a91279b), 10.7-9/10 (f0d881d), 10.7-11 (c6042af).

## Phase C — architecture: DONE
10.7-12 (84bf081), 10.7-13 (c24cdc9), 10.7-14 (797cbd6). No migration (chat_sessions
already had project_id + sidebar already shows project label).

## Phase D — logo: DONE
10.7-15 (d71a140) — green-bg circle removed from project-chat avatar.

## Phase E — self-test: typecheck (web/shared/api) PASS, prod build PASS.
Visual CDP walk DEFERRED — Chrome :9222 not running. Final report:
SPRINT_10_7_COMPLETE_2026-06-03.md.

ALL 15 ITEMS COMPLETE. 12 atomic commits 1247046→d71a140.
