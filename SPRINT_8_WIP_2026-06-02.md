# Sprint 8 — Work In Progress Log

Started 2026-06-02. Autonomous overnight pass. Quality bar 9.5/10.

## Running log

- [00:00] Phase 0 start. Chrome:9222 + dev:3000 BOTH down initially.
- [00:05] Self-started prerequisites: `pnpm dev` (web 3000 + api 3001 via tsx), launched Chrome with `--remote-debugging-port=9222` using repo `.chrome-debug-profile`. Verified: 9222 returns JSON, web 200, api /health 200. NOT a STOP — brought env up myself.
- [00:08] sprint-8/ screenshot dirs scaffolded.

## Phase A items (status)
- [x] 5.1 Logo Sweep — DONE (commit 15dfaec). 12 files migrated, goblin-mark.tsx deleted, typecheck clean, 404 verified showing new gold g-mark. Save/stream/build moments to be captured during 5.5/5.6 (need live streaming).
- [x] 5.2 Persistent Vercel Link — DONE (commit a60dfd8). Session live-URL card (persists via detail.deployUrl, survives reopen) + hub DeployUrlList (copy+timestamp). Backend: GET detail returns deployUrl, new GET /projects/:id/deployments, deploy logs to deployments table (migration 0056, latent), graceful preview_url fallback. VERIFIED live (repointed web→local API).

## ENV REPOINT (active — must revert at handoff)
- apps/web/.env.local line 3: NEXT_PUBLIC_API_URL → http://localhost:3001 (was Railway prod). Next reads apps/web/.env.local, NOT root .env.local. Needed so new backend endpoints are exercised live. **Revert to https://goblinapi-production.up.railway.app before handoff.**
- New backend (0056 migration + endpoints) is LATENT on prod until founder applies 0056 + redeploys Railway. All code degrades gracefully (preview_url fallback) so prod keeps working today.
- [ ] 5.3 Project Hub as Daily Dashboard
- [x] 5.3 Daily Dashboard — DONE (2cbf9b6). Recent chats + sessions cards, 2-col responsive, server-side queries. Verified desktop + mobile.
- [x] 5.4 File Explorer — DONE (924b525). /files route, tree+preview+upload+download+delete, mobile. Backend: listFilesWithMeta/getFileBytes/uploadProjectFileBytes + files-tree/files-raw/upload endpoints. Verified live: list, code preview (CM readonly), image preview, upload (backend 200), delete (browser confirm→200→refresh), mobile. NOTE: fixed an infinite-render loop (useEffect dep) during build.
- [x] 5.5 Undo/Redo — DONE (0ecbdc8). Visible buttons wired to CM history; editor stays mounted across AI boundary (live stream = overlay) so AI gen = one undo event. Verified live: type→undo removes→redo restores. AI-edit-undo verified by architecture (model unavailable for live AI test).
- [x] 5.6 Live Diff — DONE (6480ffd). StreamingDiffView (jsdiff + CM decorations) in the streaming overlay for edits; new files = plain. Guarded (try/catch fallback, never breaks streaming). diffLines classification verified deterministically in node. Live visual blocked: (a) no model for real stream, (b) bare isolation route doesn't hydrate under Next-16 dev.

## BLOCKERS (environmental, not Sprint-8 bugs)
- **Model unavailable**: test account (vinc.hafner3) → "Model not found in LiteLLM" / "Fehler". No working model (free pool removed, no BYOK key). Blocks live AI generation → can't exercise real streaming for 5.5 AI-undo + 5.6 diff end-to-end. Both verified by component logic instead.
- Next-16.2.6 turbopack dev: intermittent ChunkLoadError + hydration-mismatch + "chunk.reason.enqueueModel is not a function" RSC warnings. Dashboard routes hydrate fine; a bare top-level client route did not. Pre-existing "1 Issue" dev badge = hydration warning.

- [00:20] Logo inventory done → sprint-8/logo-inventory.txt. ROOT CAUSE = `components/ui/goblin-mark.tsx` `GoblinMark` (old crude goblin-head SVG), used in 12 files incl. SessionPane save/deploy, SessionThread stream, CodeActionBar build, not-found, login, chat avatars. `GoblinLoader` already wraps NEW GoblinLogo (fine).
- [00:30] Phase 0 4.2 baseline: logged in (profile retained session, user vinc.hafner3). Hub renders. Code tab /work?tab=code renders Sprint-7 SessionTabs (NOT classic fallback), shortcuts + dark toggle present. NOTE: first /work hit 404'd — diagnosed as Turbopack cold-start race (subsequent hits 200, project query fine). NOT a Sprint-7 regression. Hydration-mismatch warning observed in dev log (cosmetic, page renders) — noted for Phase D.
- [00:30] Confirmed old logo renders live on not-found page (sprint-8/full-walk 404 shot).

## Item ordering decision
Logo Sweep (5.1) FIRST. Justification (§9c): items 5.5 (undo/redo buttons) + 5.6 (live diff) add new UI to code components; sweeping first means new code uses GoblinLogo from the start, no rework. Then 5.2→5.3→5.4→5.5→5.6.

## Phase 0 environment caveats
- Web (localhost:3000) → PROD Railway API + PROD Supabase (founder's setup, act as test user only).
- 58 [E2E-TEST] projects polluting sidebar (test data, not a bug to fix).
- Red "1 Issue ✕" dev badge bottom-left — investigate origin during Phase A/D.
