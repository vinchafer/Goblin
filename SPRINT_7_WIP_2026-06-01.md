# Sprint 7 — WIP log (2026-06-01, autonomous overnight)

Build the full Code-Tab vision: multi-session, in-tab AI composer, streaming agent,
mobile-first. Founder accepted higher risk for greater output.

## Critical environment constraint (decided Phase 0)
- Web (`.env.local`) talks to **PROD Railway API** (`goblinapi-production.up.railway.app`).
- Local API (`:3001`) and prod API both use the **same PROD Supabase**
  (`ogrkollxnoawfdkzdmtn.supabase.co`). No local Supabase, no supabase CLI.
- Migration 0055 is **founder-applied only** (non-negotiable). Therefore the new
  `code_sessions*` tables do not exist anywhere tonight →
  **multi-session backend + streaming agent CANNOT be runtime-tested tonight.**
- Consequence: I build the full vision, verify with typecheck + production build +
  careful static review, and ship a **founder verification script**. Live E2E lights
  up after founder runs `supabase db push` and pushes the API.

### Safety decision (protects Sprint-6 win)
The multi-session layer is **additive + degrades gracefully**: if the sessions API
is unavailable (404 / no table), the Code Tab falls back to the Sprint-6 light
editor + Zwischenraum. No regression to the shipped feature, ever.

### Verifiable-tonight (no DB needed)
- Phase 4 density audit (pure CSS, CDP)
- Phase 5.2 footer pages (pure frontend, CDP)
- Phase 5.3 chat-button fix (pure frontend, CDP)
- Phase 6 Sprint-6 no-regression walk

## Log
- [start] Phase 0 — env verified: :9222 JSON ✓, :3000 200 ✓, HEAD a02f185 ✓.
  Read architecture doc, tokens, Sprint-6 reports, current code-tab + hooks +
  backend patterns (chat SSE, auth, supabase admin, file-storage, deploy).
- [start] Mental model re-confirmed: (iv) Hybrid. No design flaw found. Proceeding.

- Phase 1+2 (8303058): migration 0055 + code-sessions route (SSE) + parser;
  frontend hooks (sessions/agent/detail) + 7 components + workspace↔classic switch.
  typecheck ×2 + next build green. Backend not runtime-testable (founder applies).
- Phase 3 (c89d904): SessionPicker dialog (in 8303058), draft Kopieren/Verwerfen
  review actions, full-flow e2e harness (SPRINT7_LIVE gated).
- Pivot → verifiable-tonight phases (no DB needed): 5.3 chat-button, 5.2 footer,
  4 density, 5.1 activity. These get live CDP verification.

- Phase 5.3 (5adb6b8): chat-button — circular --bone token fix. VERIFIED LIVE
  (computed color rgb(244,236,216) bone-on-green).
- Phase 5.2 (850d94a): /about /manifesto /changelog + footer cleanup + middleware
  public. VERIFIED LIVE (200, screenshot).
- Phase 5.1 (1b69924): unified activity feed (chat+deploys).
- Phase 4 (c7446d6): density compressed — section-title 700→600 + audit report.
- Phase 6: typecheck api+web green, production build green (32 pages, new routes
  static), Code-Tab no-regression VERIFIED LIVE (probe 404 → classic editor renders).
  SPRINT_7_COMPLETE + SESSION_HANDOFF written. DONE.
