# Sprint 6 — WIP Replay Trail (2026-06-01)

Factual, terse. One sprint, 7 phases. Code Tab re-imagine + workflow build.

## Log

- [P0] Env verify: :9222 was DOWN → launched Chrome myself (fresh temp profile `%TEMP%\goblin-cdp-profile`, PID bound, `Chrome/148.0.7778.181`). :3000 → 200. HEAD `976b941` (Sprint 5, local-only). Did NOT stop the sprint — Phase 1 is design-only, browser only needed from verification phases on.
- [P0] Authoritative inputs: `GOBLIN_DESIGN_SYSTEM.md` + `GOBLIN_ARCH_v6.md` do NOT exist as files in repo. SSOT in code = `apps/web/styles/design-tokens.css` v1.1. Worked from that.
- [P0] Current Code-Tab inventory done. Key files: `components/project/code-tab.tsx`, hook `hooks/useCodeTab.ts` (orchestrator) → sub-hooks `useCodeFiles / useCodeVercel / useCodeGitHub / useCodeTabs / useCodeInjections`. Surfaces: `CodeActionBar` (Push GitHub + Build), `CodeFileTabs`, `CodeFileTreePanel`, `CodeMobileFileSheet`, `CodeEmptyState`, `InjectedBanner`.
- [P0] **Root problem confirmed in code:** `InjectedBanner.tsx` renders "Review & Apply" RIGHT NEXT TO "Build" (deploy) — one tap from injected code to live deploy. No Save↔Deploy separation. No in-tab prompt input. No multi-session. No ModelPicker. Editor surface hardcoded dark (`--surface-ink-2 #28251D` / `--green-950 #08170F`) = Vincent's "extrem dunkel".

## Founder walkthrough findings (logged, Section 5.3)
- "Neuer Chat" button + text both green = unreadable → Phase 6.1
- No project-level overview of recent chats/sessions → Phase 5
- Chat code-block buttons size-imbalanced (tiny Copy / huge An Code senden) → Phase 3.1
- Code Tab "extrem wenig Handlungsspielraum", Review&Apply deploys instantly → Phase 1+2
- Need explicit Save step BEFORE Deploy → Phase 2.4
- No ModelPicker in Code Tab → Phase 2.3
- AI returns prompt (not code) → Code Tab should run it with selected model → Phase 2.3

## Token facts (from design-tokens.css v1.1)
- LOCKED: brand-green `#1A3A2A`, ink-deep `#0F2B1E`, brand-gold `#D4A737`, bone `#F4ECD8`, paper `#FBF7EC`
- Light surfaces: surface-1 `#FBF7EC`, surface-2 `#F4ECD8`, surface-3 `#E8DEC2`, surface-4 `#D8CBA8`
- Warm-dark: surface-ink-1 `#3F3A2C` (proposed dark-mode editor base, NOT the extreme #28251D/#08170F)
- `[data-theme="dark"]` flip system already exists → dark-mode option builds on it.

## Phase 1 — DESIGN (gated)
- Mental model chosen: **(iv) Hybrid** — parallel Sessions as top tabs, each = chat-thread + work surface. Justified: literally Vincent's "ein Chat-Fenster und ein Claude-Code-Terminal, alles in einem" + "neue Tabs aufmachen". One model, two renders (desktop split / mobile single-column push).
- Core invention: **change state machine** Entwurf → (Sichern) → Gesichert → (Veröffentlichen) → Veröffentlicht. Deploy gated on Saved. This IS the missing Zwischenraum. No auto-deploy ever.
- Deliverables: `CODETAB_REIMAGINE_ARCHITECTURE_2026-06-01.md` (sections A–J), `CODETAB_PROPOSED_TOKENS_2026-06-01.md`, 6 mockups in `sprint-6/code-tab/*.html` + `.png`. All open cleanly in Chrome (CDP-verified). Light editor surface = `--surface-1 #FBF7EC`; dark option retuned to `--surface-ink-1 #3F3A2C` (not the old black).
- Phase 1 added NO app/TS code (docs + standalone HTML only) → typecheck unaffected. Gate met. Per brief: design commits WITH Phase 2 Slice 1.
- Migration: DELETE `InjectedBanner` (loud banner + adjacent Build = the anti-pattern). KEEP foundation (`useCodeFiles`, `useCodeVercel`, CodeMirror engine, `/files/*` routes).

## Phase 2 + 3 + 6 — IMPLEMENTATION (committed)
- `fd6b634` Slice 1: **light editor surface** (default) + retuned warm-dark option. CodeMirror gets `goblinLight`/`goblinDark` themes with real HighlightStyle on v1.1 anchors (added deps `@codemirror/language`,`@lezer/highlight`). Scoped `--ed-*` tokens via `data-editor-theme` flip the whole Code-Tab chrome (tabs, tree, file bar, empty state, action bar, mobile sheet). `useEditorTheme` persists; toggle in action bar. **Verified live: light + dark both render, no black hole.**
- `b0861bf` **Zwischenraum**: InjectedBanner reworked → code lands as Entwurf draft (Ansehen & Sichern / Verwerfen, NO adjacent deploy). Deploy = separate confirmed step ("Veröffentlichen? …"). DiffModal re-skinned light + German. Verified live (confirm dialog works both themes).
- `63f3eb0` Phase 3.1: chat code-block buttons rebalanced — equal-size footer bar, hierarchy by variant (Copy ghost / An Code senden gold-outline secondary).
- `357e690` Phase 6.2/6.3: hero title 2-line clamp + overflow-wrap (long E2E names dominated); wordmark → 18.66px (large-text 3:1 contrast).

### Decisions / deferrals
- Phase 6.1 "Neuer Chat green-on-green": could NOT repro precisely — 'Neuer Chat' only appears as a chat-title fallback + small icon buttons (all readable). DEFERRED, needs founder to name the exact screen.
- **Scope call (honest):** full multi-session + in-tab AI composer/agent (Slices 2/3/6/7/8) is a 10h+ streaming build, high-risk to ship blind overnight into THE defining feature. Prioritised the two hardest founder complaints (light editor + Zwischenraum) + the small visible wins, all green commits. The thread/agent system is fully specced in the architecture doc as the clearly-staged next build the founder can test live.
- Phase 5 (activity overview): project hub ALREADY renders Aktivität / Letzte Deploys / Dateien / URLs sections (seen live). Likely largely done pre-sprint — verify in Phase 7, polish only.
