# Screen 04 — v1 → v2 (Mockup-Only Iteration)

Review-driven revision. Mockup-only — keine Source-Edits. Datei: `built_04_v2.html`.

## Die fünf Änderungen

| # | Change | v1 | v2 |
|---|--------|----|----|
| 1 | **Komposition** | `justify-content:center` (vertikal zentriert) | `flex-start` + `padding-top` 120px (Desktop) / 64px (Mobile) → Greeting im oberen Drittel |
| 2 | **Mobile Mode-Tile** | `[ham][g-mark][mtile][spacer][+][avatar]` | `[ham][g-mark][spacer][mtile][+][avatar]` — rechtsbündig, matcht v7 |
| 3 | **`.chat-scroll--empty` Modifier** | hardcoded `justify-content:center` auf `.chat-scroll` | Base `.chat-scroll` top-aligned (für 05/06), Empty-State über `.chat-scroll--empty` Modifier. Element trägt beide Klassen. |
| 4 | **Greeting** | "Was möchtest du heute bauen?" | "Leg los." (gleiche Typo, kürzer, direkter) |
| 5 | **Mark-Variante** | `--brand-green` (#1A3A2A) | `--ink-deep` (#0F2B1E) — §B1.4 "reversed on warm light" |

## Diff-Bestätigung Change 2

v7-Mobile-Header vs v2-Mobile-Header: **einzige Differenz = Crumb/Title-Content** (v7 zeigt Mode-Tile gleichen Aufbau), **nicht** die Element-Reihenfolge. Reihenfolge identisch:
`ham → logo-btn → spacer → mtile → fab → avatar`.

## Forward-Note (Change 3)

Das `.chat-scroll--empty` Pattern signalisiert dem späteren Code-Build die intended Struktur:
- `.chat-scroll` bleibt top-aligned default → Screen 05 (Message-List, scroll-bottom-anchored) funktioniert ohne Override.
- Empty-State (`messages.length === 0`) hängt nur den Modifier an.

## Unverändert (bewusst)

- Composer-Placeholder (Desktop "Beschreib in einem Satz…", Mobile "Was möchtest du bauen?…") — Change 4 betrifft nur Greeting.
- Komplette Shell-CSS (Header, Sidebar, Composer) verbatim aus v1/v7.
- Chips, Daten, Tokens — keine neuen.

## Future-Items (nicht in v2 angefasst)

Keine. Falls beim Build was auffällt → hier nachtragen, nicht still mitfixen.
