# Sprint 3 — Work-In-Progress Log (2026-05-31)

Beta polish. 13 items / 6 phases. Sequential phases, atomic commits, no push.
Continuing same session as Sprint 1+2 — budget-aware; will ship solid items and document
deferred ones honestly rather than do all 13 shallowly.

Note: `pnpm dev` → Railway PROD (shield dormant by founder choice). Browser verification hits
prod; keep reads-only / test-user.

## Phase 0 — Quick wins
- [start] Re-oriented. Architecture known from Sprint 1+2. Starting with code items
  (R2 copy, R3 SSO hint, B13 imprint) — fast + build-verifiable; R5 smoke needs browser.
- [P0] R2 done: reassurance copy under /dashboard/new CTA. Committed.
- [P0] R3 done: SSO-protection hint under preview-tab URL bar (preview-tab = persistent post-deploy surface, chosen over transient deployMessage). Used confirmed --t-small/--ink-3 tokens (not unconfirmed --t-caption per SSOT non-negotiable). Committed.
- [P0] B13 done: /imprint already existed (EN, env-var placeholders — better than source placeholders). Added responsible-party, EU ODR dispute clause, VAT-threshold note. Kept EN (sibling legal pages + B6 canon legal=EN); documented B13(DE-ask)-vs-B6(EN-canon) → chose EN consistency. Committed.
- [P0] R5 BLOCKED for automated screenshots: browser-harness needs Chrome remote-debugging enabled (manual founder step; founder asleep). Playwright (own browser) works but budget-heavy. localhost:3000 IS up (200). R5 component typecheck-clean + follows existing ConnectorsPage patterns + backend live-verified Sprint 2 → deferring live visual smoke, documenting.

## STRATEGY ADJUSTMENT (budget + browser-harness blocked)
Browser-harness unavailable (remote-debugging not enabled). Blocks automated R5/B9/B10/B11/B12/R1 visual+Lighthouse+axe work. Same session as Sprint 1+2 → finite budget. Prioritizing read-only/code items completable at quality without a browser:
- B15 connectors verification (read-only trace; incl. GitHub decrypt-bug check vs Sprint-2 Vercel bug) — HIGH value.
- B6 language unification (grep-based scan + targeted fixes; typecheck-verifiable).
- B14 error-handling code review (verify error paths exist; full manual test needs browser).
- R4 pgcrypto read-only infra check.
Browser-dependent items (R5 screenshots, B9/B10 Lighthouse, B11 axe, B12 mobile, R1 Playwright UI) → documented as deferred with rationale in final report. Honest PARTIAL.

## DONE — Sprint 3 PARTIAL (7/13 shipped)
- Shipped: R2 (88df954), R3 (9ba8659), B13 (92370d4), B15 (802c586), B6-partial (434b9cf), B14-partial (a76fa84), R4 (3af91fa).
- B15: GitHub has NO Vercel-style decrypt bug (matched encrypt/decrypt) — highest-risk question cleared.
- R4: pinned root cause = search_path bug in migration 0043 (gen_random_bytes unqualified, pgcrypto in extensions schema). One-line fix, NOT a missing extension.
- B12: static-clean (markup + CSS) — collapse needs live 390px inspection; not blind-fixed.
- Deferred (browser-harness needs chrome remote-debugging enable, founder asleep + budget): R5, B9, B10, B11, B12, R1. All have concrete next steps in SPRINT_3_COMPLETE.
- typecheck PASS throughout. No push.
