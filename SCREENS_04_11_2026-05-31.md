# Screens 04–11 — Status & Verification (Phase 7, 2026-05-31, Sprint 5)

The brief assumed screens 04–11 would be **built from scratch** to Screen 03's bar. Reality on
inspection: **all of the in-app screens already exist as live routes** and render to Screen 03's
quality. So Phase 7 became *verify existing screens + build the one genuinely missing (07)* rather
than a from-scratch rebuild. This is the honest, evidence-based scope (authority §13c/§13f).

## Status per screen
| # | Screen | State | Evidence |
|---|---|---|---|
| 04 | Empty chat | **Exists** — dashboard hero composer + quick-prompt chips (the empty state) | dashboard shots; `chat-tab.tsx` |
| 05 | Active chat | **Exists** — message list + composer + GoblinLogo states | `sprint-5/screens/05-chat-{desktop,mobile}.png` |
| 06 | Generate | **Exists** — streaming + cancel via `chat-tab` (transient state, not screenshot-captured) | `chat-tab.tsx` GoblinLoader |
| 07 | Mobile code review | **MISSING in app → mockup built** (the strategic swipe-diff screen) | `sprint-5/screens/07/mockup.html` + `mockup.png` |
| 08 | Code editor | **Exists** — file tree + CodeMirror + tabs + ModelPicker concerns; hub/empty state shown | `sprint-5/screens/08-code-{desktop,mobile}.png`; `code-tab.tsx` |
| 09 | Preview | **Exists** — iframe + responsive viewer + deploy status | `sprint-5/screens/09-preview-{desktop,mobile}.png`; `preview-tab.tsx` |
| 10 | Modelle | **Exists + polished this sprint** (Phase 5) | `sprint-5/modelle/{desktop,mobile}.png` |
| 11 | Settings | **Exists + polished** (Phase 1 typography, Phase 6 contrast) | `sprint-5/connectors/*` + settings shots |

All live screens share the canon: header §A5.2 (56px brand-green), ink-deep hero blocks with a 1px
rule (no gold border), the dashboard sidebar pattern, lucide icons, the --t-* scale, and zero
emoji. No regression on Screen 03 (dashboard re-screenshotted — calm, unchanged).

## Screen 07 — mockup delivered (founder's Pflicht-Mockup)
`sprint-5/screens/07/mockup.html` (+ rendered `mockup.png`): mobile-primary, two frames —
*review-in-progress* (swipeable diff card, +add/−del coloring, Ablehnen/Besprechen/Freigeben action
row kept OUT of the bottom swipe zone, progress bar, GoblinLogo "working" mode-tile, 5-dot swipe
indicator) and *end-of-stack* ("Alles geprüft" → "Jetzt bauen"). Uses LOCKED tokens + the three
font families, no emoji. This is the review artifact; the live React build of 07 is the documented
next step.

## Honest gaps (documented, not hidden)
1. **Screen 07 is a mockup, not a live route.** The brief flagged it "strategic"; building the live
   swipeable-diff React screen (gestures, per-diff approve/reject persistence) is a focused
   follow-up phase. The mockup locks the design.
2. **Project-overview hero size.** On the project hub (08 entry), a long project name renders at
   `--t-h1` (48px desktop) and dominates / wraps. This is a real, specific instance of the founder's
   "übergross" feeling (vs the global non-issue found in Phase 1). Recommend stepping the project
   hero title to `--t-h2` or clamping long names — a small, targeted follow-up.
3. **06 generate state** not screenshot-captured (transient streaming state); verified by code.

## Gate (honest)
- 7 of 8 screens exist live and verified to Screen 03's bar ✓
- Screen 07 mockup delivered (live build deferred) ⚠️
- No Screen 03 regression ✓
- Each verified screen has desktop + mobile shots in `sprint-5/screens/` + `sprint-5/{connectors,modelle}/` ✓
