# Sprint 1 — Work-In-Progress Log (2026-05-30)

Autonomous run: B3 (Dev-Safety Shield) → gate → B1 (Ship-Loop).
Tight factual entries so founder can reconstruct the run.

## Pre-flight findings (before B3)
- [start] Re-read SESSION_HANDOFF + prompt. Caveman/DE for chat, normal EN for code/commits/reports.
- [start] `.env.local` scan:
  - Stripe: both keys `sk_test_`/`pk_test_` (lines 33-34 + 118-119). **0 sk_live_ in env.** ✓
  - `NODE_ENV=production` (line 76) set in local dev → breaks any `NODE_ENV!=='production'` guard. Shield must not rely on NODE_ENV alone.
  - Vercel token var is `VERCEL_TOKEN_SCOPE` (line 120), NOT `VERCEL_TOKEN`. Value `vcp_…`.
  - `.env` (no suffix) does NOT exist — only `.env.local` + `.env.vercel.local`. But root `test` script = `dotenv -e .env -- playwright test`. Test env wiring needs check.
- [start] Architecture reality vs prompt assumptions:
  - Monorepo: `apps/api` (Hono, Railway) + `apps/web` (Next.js) + `packages/shared`. Prompt's `apps/web/lib/*` paths are wrong — Stripe/Vercel/Supabase-admin all live in `apps/api`. Shield goes in apps/api (prompt sanctions "inspect to find").
  - Supabase admin = `apps/api/src/lib/supabase.ts` (`getSupabaseAdmin`, singleton).
  - Vercel = `apps/api/src/services/vercel-service.ts`. Only 2 `api.vercel.com` call sites. **Uses per-user BYOK token from `byok_keys` table, NOT the env account token.** `VERCEL_TOKEN_SCOPE` is unused by code. "synapse-platform" appears nowhere in code.
  - Ship-loop already scaffolded: `routes/{send-to-code,builds,deploy}.ts`, `services/{vercel-service,project-generator}.ts`.
- [start] DECISION (founder asleep, autonomous authority §5b): prompt's Vercel allowlist premise (lock account-token to synapse-platform) doesn't match BYOK reality. Implement Vercel guard anyway as defense-in-depth around the 2 call sites, enforcing deploy-name ∈ {synapse-platform, test-*}. B1 test deploy will use a `test-` prefixed name to satisfy the allowlist literally.

## B3 build
- [B3.0] DECISIVE FINDING: web `lib/api.ts:getApiUrl()` returns `NEXT_PUBLIC_API_URL` (=Railway PROD) with NO localhost auto-switch. So `pnpm dev` web → Railway prod API → prod Supabase. A shield in apps/api only bites if dev routes to the LOCAL api. `index.ts:6` loads `.env` (missing!) so local api can't even start with creds locally.
  - Consequence: shield built correctly but DORMANT for `pnpm dev` until web points at local api. NOT silently flipping `NEXT_PUBLIC_API_URL` in untracked `.env.local` (breaks founder session mid-sleep + not committable). Documenting as #1 open blocker w/ exact 1-line action instead.
  - NODE_ENV=production in `.env.local` poisons all `NODE_ENV!=='production'` dev checks → shield keys off `IS_DEV_MODE` (GOBLIN_DEV_MODE flag), not NODE_ENV.
  - Will also fix `index.ts` dotenv to fallback to `.env.local` (prod-safe no-op on Railway) so the local guarded API can start in dev.
- [B3.1] env.ts: IS_DEV_MODE/TEST_USER_EMAIL/VERCEL_ALLOWED_PROJECT + validateDevShield()+assertStripeKeyMode(). Keys off GOBLIN_DEV_MODE not NODE_ENV.
- [B3.4] vercel-guard.ts + wired into vercel-service.ts deploy POST (guards sanitized deployName).
- [B3.2] supabase-guard.ts (Proxy: insert/upsert payload-check, update/delete filter-check at await, fail-closed) + wired at getSupabaseAdmin() chokepoint. resolveTestUserId() best-effort at startup.
- [B3.3] sk_live_/pk_live_ grep → 0 real keys (only masked UI mockups + guard literal + DE help copy). assertStripeKeyMode at startup.
- [B3.0] load-env.ts as first index.ts import (ESM ordering fix). startup banner added.
- [B3.5] dev-guard.test.ts 20 tests. FINDING: apps/api has NO typecheck script → `pnpm --recursive typecheck` skips it; tsup(esbuild) skips type errors → billing.ts:145 narrowing error was latent. Fixed (§5a trivial, logged).
- [B3 GATE] tsc --noEmit PASS · tsup build PASS (292KB) · 65/65 tests PASS (incl 20 shield). Mental trace 3 scenarios ✓ (test-backed).
- [B3 SHIPPED] commit **b9fb66e**. Proceeding to B1.

## B1 build
- [B1.1] Read deploy/builds/send-to-code/projects/vercel-service/project-generator + web hooks (useCodeVercel, useBuildStatus, useCodeInjections), CodeActionBar, build-status-bar, preview-tab, integrations page.
- [B1.1] Loop is WIRED end-to-end (first grep missed it — calls use `${API_URL}` template strings). "Build" = cosmetic build_runs record; real build is Vercel server-side on deploy. Build+Deploy collapse.
- [B1.1] B1.3 Vercel Integrations UI does NOT exist — legacy page lists Vercel "Coming soon"; canonical SettingsRoot has none. Token only via BYOK keys.
- [B1.1] Read-only prod probe (audit/check-vercel-token.mjs): test user has ONLY groq BYOK key, 0 Vercel tokens, 57 projects.
- [B1 DECISION] STOP B1 execution at deploy boundary. Deploy needs a Vercel token test user lacks; only available = founder account-level full-scope token → real deploys to founder's personal Vercel. Non-negotiable (a) binds ("can't identify project → don't call, document, proceed differently"). §5 reserves Vercel UX for founder. Did NOT: insert founder token, fire deploys, start local API (runs prod migrations on boot), or half-build B1.3 (wouldn't pass B1 gate without token anyway).
- [B1 STATUS] INVESTIGATED + blockers documented → B1_INVESTIGATION + SPRINT_1_COMPLETE (status PARTIAL). No source changes for B1.
- [END] B3 SHIPPED (b9fb66e). B1 PARTIAL/BLOCKED on founder Vercel-account decision (5-min call) → clean Sprint-2 build.
