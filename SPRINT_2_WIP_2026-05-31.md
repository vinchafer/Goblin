# Sprint 2 — Work-In-Progress Log (2026-05-31)

Autonomous run. Phase 0 (migrations audit) → 1 (shield activation) → 2 (Vercel setup) →
3 (ship-loop proof) → 4 (carry-over B7/B8/B2/B5). Gates between phases. No push.

## Phase 0 — Migrations Idempotency Audit
- [start] Re-oriented from Sprint 1 (same session). Architecture known: web→Railway prod API; mutations in apps/api; B3 shield at getSupabaseAdmin chokepoint.
- [P0] DECISIVE FINDING: `runStartupMigrations()` (apps/api/src/startup-migrations.ts) does NOT run any migrations. It does 2 read-only SELECTs (users/projects schema validation), logs warnings. No DDL/INSERT/UPDATE/DELETE. Migrations applied out-of-band via `supabase db push`. Founder's concern (destructive migrations at boot) is unfounded — boot is read-only.
- [P0] `startCron()` (lib/cron.ts) = no-op unless ENABLE_CRON=true (NOT set in .env.local). No boot-time jobs. (hardDeletePendingAccounts is destructive but cron-gated + goes through guarded admin client.)
- [P0] Boot-path prod-write surface in dev = NONE (cron off, migrations read-only, resolveTestUserId reads only). Booting local API against PROD performs only SELECTs → safe. No targeted skip needed.
- [P0] Still honoring founder's idempotency request via static scan of the 53 migration files (relevant to the separate `supabase db push` path, NOT the boot path).
- [P0 GATE] typecheck+build PASS. No dev-skip needed. Committed `e698475`.

## Phase 1 — Shield Activation
- [P1.1] Vercel probe (audit/vercel-probe.mjs): new VERCEL_TOKEN_SCOPE = test account `vinchafner2-1996` (vinc.hafner2@gmail.com), team `vincent-2-s-projects`, 1 placeholder project `project-kiy64`. Token 200 OK. Set VERCEL_ALLOWED_PROJECT='project-kiy64' (guard already allows ALLOWED || test-*). Updated vercel tests.
- [P1.2] supabase-guard: added async project→owner resolver. UPDATE/DELETE filtered by id/project_id (not user_id) now resolves row owner via 1 cached read (resolveOwnerIsTestUser in supabase.ts), allows iff owner=testUser. Fixes deploy.ts:64 + builds.ts:118 fail-closed. +5 tests (25 total).
- [P1.3] api.ts getApiUrl: unset NEXT_PUBLIC_API_URL → localhost:3001 in dev, Railway in prod; explicit always wins. apps/web/.env.example documents toggle. docs/DEV_SAFETY.md updated (allowlist, resolver, activation).
- [P1 GATE] api typecheck+build PASS, web typecheck PASS, 25/25 dev-guard tests PASS.
- [P1] ⚠️ Founder action still required: set `.env.local` NEXT_PUBLIC_API_URL=http://localhost:3001 (or remove it) to arm shield for pnpm dev. Not auto-flipped (untracked, would break session).

## Phase 2 — B1 Vercel setup (backend)
- [P2.0] BUG found: 'vercel' not in LLM ByokProviderSchema; vercel-service decrypted with simple decryptData but createKey stores v2/v1 → tokens were undecryptable. Kept 'vercel' OUT of LLM enum (avoid model-list pollution); added dedicated connection helpers.
- [P2.0] byok-service: getActiveKey → getActiveKeyByProvider(string) (canonical v1/v2 decrypt); + validateVercelToken/storeVercelToken/getVercelConnection/disconnectVercel.
- [P2.0] vercel-service.getUserVercelToken now uses getActiveKeyByProvider('vercel') — decrypt fix.
- [P2.0] routes/integrations.ts (GET/POST/DELETE /api/integrations/vercel), mounted.
- [P2.1] Stored test-user Vercel token via real storeVercelToken (tsx script). Round-trip verified: getVercelConnection → connected:true, account vinchafner2-1996. (v2 vault failed gen_random_bytes missing → v1 fallback works.) Guard allowed test-user writes.

## Phase 3 — Ship-loop proof
- [P3] ship-loop-proof.ts (real services): create project → uploadFile → deployToVercel → poll → fetch URL → cleanup.
- [P3] First run: deploy READY but URL 401 = Vercel Deployment Protection (new team SSO default). Added PATCH ssoProtection:null on own test- project.
- [P3] PROOF PASS in 14.0s: live URL HTTP 200 + hero text. Cleanup: Vercel delete 204, storage+row removed, no orphans. proof.json + deployed-page.html in ship-loop-proof/.
- [P3] Caveat: backend/services-level E2E (real deploy), NOT browser-UI click-chain. 8 UI screenshots NOT captured (deferred — Playwright UI run = Sprint 3 follow-up). UI wiring code-verified Sprint 1.
- [P2/P3 GATE] typecheck+build PASS, 25/25 dev-guard tests PASS. Committed 554c6d3 (backend+proof) + 4abf38d (UI).

## Phase 4 — Carry-over
- [B7] 75e88dc: /dashboard/settings index → redirect /dashboard (kills hang). §7.1 vs §12 conflict resolved toward §7.1 (redirect retires dead page).
- [B8] c438d93: branded not-found.tsx (page already notFound()s; Sprint-1 "login bounce" was unauthenticated case).
- [B2] 818b1b0: /register → /login?mode=signup (login already has full signup mode + visible switch; no form dup).
- [B5] 57ced67: verified first-build-free (trial-gate auto-starts 3d trial + passes; BYOK/reads never gated). Reassurance copy = ready-to-apply doc.
- [P4 GATE] web typecheck PASS. 4 atomic commits.

## DONE — Sprint 2 COMPLETE
All 5 phases shipped (8 commits, none pushed). Headline: ship-loop PROVEN (live 200 in 14s). SPRINT_2_COMPLETE_2026-05-31.md written. Founder action: flip NEXT_PUBLIC_API_URL→localhost:3001 to arm shield.
