# Sprint CHAT-IO — Diagnosis (per-unit "what the code actually does today")

Branch `chat-io-2026-07-06` off master `02555de`. Diagnosis before build (HARD RULE 2).

## C1 — Dictation
The mic is a **cosmetic stub**. `VoiceButton` (`apps/web/components/chat/ChatInput.tsx:306-374`) opens a `MediaRecorder` but registers **no `dataavailable` handler**, so no audio is captured; `mr.onstop` has a `// Real transcription would go here` no-op and the passed `onTranscript` callback is **never called**. A 1200 ms `setTimeout` shows a fake "Goblin hört zu…" label, then resets. A fully-working `useVoiceInput` SpeechRecognition hook exists at `apps/web/hooks/use-voice-input.ts` but is **imported nowhere** (dead code; hard-coded `en-US`, 10s auto-stop). Textarea key handling (`ChatInput.tsx:536-542`) only `preventDefault`s on Enter-without-Shift → does **not** block iOS native keyboard dictation. DeepInfra is OpenAI-compatible (`getGoblinHostedConfig()` → `{baseURL, apiKey}`, `apps/api/src/services/goblin-hosted.ts:239`) → Whisper via `client.audio.transcriptions`. New Hono routes register in `apps/api/src/index.ts` (import block ~:75, mount block ~:206) with `authMiddleware`.

## C2 — File attach
Attachments **never reach the model**. `handleFilesPicked` (`ChatInput.tsx:607-618`) only appends a `[Anhang: <names>]` text marker — file is never read/uploaded. Send payload is `{ projectId, message, modelSlug, clientMessageId }` (`apps/api/src/routes/chat.ts:53-55`); the message array (`model-router.ts:494-503`) is **string-only** (no multimodal). No PDF/text extraction util exists; **no PDF lib** in `apps/api/package.json` (has `jszip`, `openai`, `@aws-sdk/client-s3`). E7 (`goblin-chat-system.ts:72`) treats content as known only if in the "Dateiinhalte" section **or written/pasted in this conversation** → injecting extracted text as user-turn content satisfies E7 with no prompt change. Reduced-context retry (`token-limit-retry.ts`) drops injected project-file contents on provider token-limit. `FILE_CONTENT_BUDGET_CHARS=48_000` at `project-context.ts:14` (separate project path, not attachments). Ledger = prose M-blocks (`docs/GOBLIN_CONSUMPTION_LEDGER.md`).

## C3 — Per-card "Ins Projekt übernehmen"
Two chat stacks: standalone/project chats use `apps/web/components/chat/CodeBlock.tsx` (the file-card; **only Copy + expand**, no send/download). Workspace stack uses `apps/web/components/workspace/CodeBlock.tsx` (already has "An Code senden" → `onSendToCode`). STC pipeline: standalone `confirmSend` (`standalone-chat.tsx:174`) stashes `sessionStorage['goblin:stc-pending']` then routes to `/work?tab=code`; `CodeWorkspace.tsx:93-103` PATCHes code-session files with `changeState:'draft'`. Project-picker sheet = `StcPreviewSheet.tsx` (shows project `<select>` when `projects` passed). P0.3 integrity = `packages/shared/src/stc-integrity.ts`; U2 classify = `apps/web/lib/file-compare.ts` (`classifyFile` → new/identical/changed). "Im Code-Bereich ansehen" toast link **does not exist yet** (sonner supports `{action}`). Card content is the in-memory assistant string parsed client-side (`parse-code-blocks.ts`) — `code`+`filename` already available in the card; no B2 key needed. `hasProject = !!projectId` (`standalone-chat.tsx:394`).

## C4 — Downloads
- **C4a** per-card download: file-card (`chat/CodeBlock.tsx`) has **no** download button today (only a message-level `handleDownload` in the standalone `Code2` dropdown that dumps the last block as `code.txt`). Card has `code`+`filename` locally → trivial add.
- **C4b** per-file download: **already implemented** (`FileExplorer.tsx:192-204` → `/files-raw/*`). "Projekt als ZIP": backend `GET /:id/download` + `createZip()` (`file-storage.ts:624`, jszip, excludes nothing special — verify `.trash/`) **exist and work**; only the **UI button is missing** (add to `FileExplorer.tsx:329-346` header).
- **C4c** print-to-PDF view: **greenfield** — no `@media print`/`@page` anywhere. Manrope via next/font (`layout.tsx`); `--font-dash-display` only under `dashboard/layout.tsx`. Ownership helper `ownsProject()` (`projects.ts:517`).

## R1 — sitemap domain — DONE (commit 56760da)
Only source ref was `apps/web/app/sitemap.ts` (5 URLs) → `https://www.justgoblin.com`.
