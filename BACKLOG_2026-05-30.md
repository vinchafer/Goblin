# Backlog — 2026-05-30 (carry-over from build bundle 11/09/08)

## Confirmed for tomorrow
- Screen 10 (ModelHub) — founder decisions in place (D3 all 8 providers + custom, D5 single shared `<ModelHub>` in both surfaces). Build prompt ready to draft.
- Screen 07 (Mobile Code-Review) — mockup-first pass, then build. Founder approved this sequence.

## Detail polish on 08 / 09 / 11 (from BUILD_11_09_08 §6–§7)
- Sub-pass A: `.settings-section p` enlarges some intentionally-small helper `<p>` (13px → 16px on desktop). Verify in Billing, About, Privacy, Notifications. If too large, add `.helper-text` / `.section-footnote` override class.
- Sub-pass A: AboutPage still contains 🇨🇭 emoji (§A6 violation). Replace with lucide glyph or remove.
- Sub-pass B: site-wide spinner audit (model-switcher, `animation: pulse` skeletons, ⟳ elsewhere). Sweep to mark states per §B1.6.
- Sub-pass C: CodeMirror `HighlightStyle.define([...tags])` for gold keywords — deferred from re-skin, optional polish.
- Inline Icon (`@/components/ui/icon`) → lucide app-wide standardisation. Hard requirement met in touched screens; this is a consistency follow-up across settings/billing/code/etc.

## Smoke-test deferred to tomorrow
- Settings desktop: all 14 sections click-through, verify `<p>` density above.
- Preview tab: GoblinLogo breath + DE empty-state + three viewports.
- Code editor: dark tokenised theme, save/dirty/inject still work (Sub-pass C logic verified untouched, but visual verification needed).
- Mobile settings sheet: identical to pre-density pilot.
